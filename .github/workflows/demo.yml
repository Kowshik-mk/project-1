name: first-workflow

on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: first-name
        run: echo "hello"
        
      - name: second-name
        run: echo "print"
